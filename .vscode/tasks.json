{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "clang++",
            "command": "clang++",
            "args": ["**/*.cpp", "-std=c++17", "-g", "-o", "m"],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "type": "shell",
        },
    ]
}

// super optimized build -> -O3 -march=native